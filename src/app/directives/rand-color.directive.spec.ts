import { RandColorDirective } from './rand-color.directive';

describe('RandColorDirective', () => {
  it('should create an instance', () => {
    const directive = new RandColorDirective();
    expect(directive).toBeTruthy();
  });
});

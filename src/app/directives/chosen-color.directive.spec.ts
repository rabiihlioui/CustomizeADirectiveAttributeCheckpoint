import { ChosenColorDirective } from './chosen-color.directive';

describe('ChosenColorDirective', () => {
  it('should create an instance', () => {
    const directive = new ChosenColorDirective();
    expect(directive).toBeTruthy();
  });
});
